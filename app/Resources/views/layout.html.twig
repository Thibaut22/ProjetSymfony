<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>{% block title %}Notes de frais{% endblock %}</title>

  {% block stylesheets %}
    {# On charge le CSS de bootstrap depuis le site directement #}
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
  {% endblock %}
</head>

<body>
  <div class="container">
    <div id="header" class="page-header">
      <h1>Plateforme de notes de frais</h1>
      
      
      </p>
    </div>

    <div class="row">
      <div id="content" >
        {% if app.user!=null %}
          <nav class="navbar navbar-default">
            <div class="container-fluid">
              <ul class="nav navbar-nav">
                  
                  <li><a href="{{ path('nf_platform_home')}}">Tableau des notes de frais</a><li>

                  {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path('nf_platform_all_user')}}">Liste utilisateurs</a><li>
                    <li><a href="{{ path('nf_platform_comparatif')}}">Comparatif des montants</a><li>
                    <li><a href="{{ path('nf_platform_add_user')}}">Ajouter un utilisateur</a><li>
                  {% endif %}
              </ul>
              <div class="navbar-right">
                <ul class="nav navbar-nav">
                  <li><a href="{{ path('nf_platform_edit_user',{'id':app.user.id})}}">Modifier profil</a><li>
                </ul>
                <p class="navbar-text "><span class="glyphicon glyphicon-user" aria-hidden="true"></span> Compte connecté: {{ app.user.username }} </p><a href="{{ path('logout')}}"class="btn btn-default navbar-btn ">Se déconnecter</a></div>
                
            </div><!-- /.container-fluid -->
          </nav>
        {% endif %}
        {% block body %}
        {% endblock %}
      </div>
    </div>

    <hr>

    <footer>
      <p> NFPlatform {{ 'now'|date('Y') }} by Chaumont Thibaut. Fais avec Symfony2 et Bootstrap</p>
      
    </footer>
  </div>

  {% block javascripts %}
    {# Ajoutez ces lignes JavaScript si vous comptez vous servir des fonctionnalités du bootstrap Twitter #}
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
  {% endblock %}

</body>
</html>